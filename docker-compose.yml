services:
  # Base de Datos
  db:
    image: mariadb:10.11
    container_name: lamerbox_db
    restart: unless-stopped
    environment:
      MARIADB_DATABASE: lamerbox_db
      MARIADB_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    networks:
      - lamerbox-network
    volumes:
      - db_data:/var/lib/mysql

  # Backend (Laravel)
  backend:
    build:
      context: ./backend
    container_name: lamerbox_api
    working_dir: /var/www
    command: php artisan serve --host=0.0.0.0 --port=8000
    environment:
      - PHP_CLI_SERVER_WORKERS=4
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/var/www
    networks:
      - lamerbox-network
    dns:
      - 8.8.8.8
      - 1.1.1.1

  # Frontend (React)
  frontend:
    build:
      context: ./frontend
    container_name: lamerbox_ui
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    networks:
      - lamerbox-network
    depends_on:
      - backend
    stdin_open: true
    tty: true
    environment:
      - CHOKIDAR_USEPOLLING=true
      
networks:
  lamerbox-network:
    driver: bridge

volumes:
  db_data: